import { menuItemType } from "src/types";
import appMenuItem from "src/app/app-menu-items";

// ==============================|| MENU ITEMS ||============================== //

const menuItems: { items: menuItemType[] } = {
  items: [...appMenuItem],
};

export default menuItems;
